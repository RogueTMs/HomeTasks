# 2. Числа трибоначчи
# (Это последовательность чисел вида 0 0 1 1 2 4 7 13 24..
# где каждый член последовательности равен сумме трех предыдущих).
# Написать программу поиска n-го числа последовательности:
#
# Ввод:
# 5
# Вывод:
# 2

In = int(input())
tri = [0, 0, 1]
while max(tri) < In:
    tri.append(tri[len(tri) - 1] + tri[len(tri) - 2] + tri[len(tri) - 3])
if max(tri) == In:
    print(len(tri))
else:
    print("Error")
    
