# Ввод:
#  Вводятся натуральные числа
# Вывод:
#  а) все элементы списка, являющиеся двузначными числами;
#  б) все элементы списка, являющиеся трехзначными числами.


In = [int(i) for i in input().split()]
n = len(In)
for i in range(n):
    if In[i] > 9 < 100:
        print(In[i])
    elif In[i] > 99 < 1000:
        print(In[i])
